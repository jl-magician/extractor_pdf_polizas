---
context: default
phase: post-v2.0
task: null
total_tasks: 0
status: paused
last_updated: 2026-04-06T19:58:57.743Z
---

<current_state>
All code is committed and pushed to origin/master. The project has shipped 3 milestones (v1.0, v1.1, v2.0) covering 17 formal phases plus one informal Phase 18. The codebase is stable with no uncommitted changes. Next steps are user-driven: uploading real PDFs to validate extraction quality across all 10 insurers.
</current_state>

<completed_work>

Milestones shipped:
- v1.0 MVP (Phases 1-5) — Foundation, ingestion, extraction, CLI, storage/API
- v1.1 API & Quality (Phases 6-12) — Migrations, export, PDF upload API, async batch, evaluator, regression suite
- v2.0 Web UI & Extraction Quality (Phases 13-17) — Pipeline fixes, web UI, HITL review, PDF reports, golden dataset tooling

Phase 18 (informal, commit 15eb54b):
- 8 financial breakdown fields as top-level schema (prima_neta, derecho_poliza, recargo, descuento, iva, otros_cargos, primer_pago, pago_subsecuente)
- Alembic migration 006
- Prompt v2.2.0 with Zurich multi-column table restructurer
- Smart re-extraction when prompt_version changes
- Evaluate buttons (single + evaluate-all) on list page
- Evaluation details UI with score cards, flagged field highlighting
- Extraction rules system with /ui/reglas management page
</completed_work>

<remaining_work>

1. Upload all 26 real PDFs and run evaluate-all to validate extraction quality across all 10 insurers (user action)
2. Create golden dataset fixtures from real PDFs: `poliza-extractor batch-fixtures pdfs-to-test/ --run-tests` (Phase 17 data gap)
3. Phase 16 plan 16-03 unchecked in roadmap: score badges on poliza list/detail, aggregate eval stats on dashboard
4. Consider next milestone planning if user wants to continue development
</remaining_work>

<decisions_made>

- Haiku used for evaluation (not Sonnet) — user's API key lacks Sonnet access
- Zurich multi-column table restructurer added to assemble_text_v2 — PyMuPDF scrambles 3-column tables
- Extraction rules stored in data/extraction_rules.json — file-based, loaded into system prompt
- otros_cargos is separate from recargo — Zurich 'Otros Servicios Contratados' != 'Financiamiento'
- fpdf2 used for PDF reports instead of WeasyPrint — Windows GTK/DLL conflict
</decisions_made>

<blockers>
- Golden dataset (Phase 17) is empty — needs real insurance PDFs from user
- Only 1 Zurich PDF tested so far — extraction quality across other 9 insurers unverified
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — full phase/milestone structure and status
2. `.planning/HANDOFF.json` — machine-readable state with decisions and pending actions
3. `policy_extractor/extraction/prompt.py` — current prompt v2.2.0
4. `policy_extractor/extraction/rules.py` — extraction rules system

## Infrastructure State
- SQLite database at data/polizas.db (with WAL mode)
- Web UI served by FastAPI + Jinja2 templates
- EVAL_MODEL_ID set to Haiku (claude-haiku-4-5-20251001)
- 26 real PDFs exist locally in data/pdfs/

<context>
The project is mature and functionally complete. All development work is committed and pushed. The main gap is validation: the system needs to be tested with real PDFs from all 10 insurers to confirm extraction quality. The golden dataset tooling exists but has no fixtures yet. The user is an insurance agency owner in Mexico processing 200+ policies/month across 10 insurers.
</context>

<next_action>
Start with: Upload real PDFs from all 10 insurers via the web UI, then run evaluate-all to validate extraction quality. After that, create golden dataset fixtures with `poliza-extractor batch-fixtures pdfs-to-test/ --run-tests`.
</next_action>
