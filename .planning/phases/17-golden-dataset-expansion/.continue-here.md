---
phase: 17-golden-dataset-expansion
task: null
total_tasks: 2
status: waiting_on_human
last_updated: 2026-03-26T04:18:42.999Z
---

<current_state>
Phase 17 tooling is 100% complete. Both plans executed successfully (batch-fixtures CLI + coverage matrix plugin). 480 tests pass. The verifier found gaps because the golden dataset itself is empty — the user needs to provide real PDFs and run the batch command to create fixtures. v2.0 milestone is the final milestone — completing Phase 17 finishes the project.
</current_state>

<completed_work>

- Plan 17-01: batch-fixtures CLI subcommand — Done (commit 16cce5a)
  - `poliza-extractor batch-fixtures <pdf-dir>` processes PDFs, extracts, redacts PII, writes golden fixtures
  - Supports `--run-tests` for self-validation, `--insurer-map` for overrides
  - 6 unit tests pass
- Plan 17-02: Coverage matrix plugin — Done (commit 6bb5f97)
  - `RegressionCoveragePlugin` in conftest.py prints insurer x type matrix after `pytest -m regression`
  - Shows all 10 insurers (Zurich, Qualitas, Mapfre, AXA, GNP, Chubb, Ana, HDI, Plan Seguro, Prudential)
  - 11 unit tests pass
</completed_work>

<remaining_work>

- User must place 20+ real PDFs (2 per insurer) in `pdfs-to-test/`
- Run `poliza-extractor batch-fixtures pdfs-to-test/ --run-tests`
- Review extractions in HITL UI, correct any errors
- Commit golden fixtures: `git add tests/fixtures/golden/ && git commit -m "feat(17): add golden dataset fixtures"`
- Run `/gsd:verify-work 17` to confirm phase goal achieved
- Run `/gsd:complete-milestone` to finish v2.0
</remaining_work>

<decisions_made>

- Phase 16: Used fpdf2 instead of WeasyPrint due to Windows GTK/DLL conflict
- Phase 16: Auto-eval triggers on any extraction when batch >= 10, 20% sample rate
- Phase 16: Swap detection integrated into existing evaluation prompt (not separate pass)
- Phase 17: Batch script creates raw fixtures, user reviews in HITL UI before finalizing
- Phase 17: 2 fixtures per insurer (20 total), most common policy types per insurer
- Phase 17: Naming convention: {insurer}_{type}_{seq}.json
</decisions_made>

<blockers>
- Golden dataset empty — needs user's real insurance PDFs to populate
</blockers>

<context>
v2.0 milestone is nearly complete. Phases 13-16 fully shipped. Phase 17 tooling done but data pending.
The user is an insurance agency owner in Mexico working with 10 insurers. They confirmed having real PDFs ready.
After golden fixtures are committed, the v2.0 milestone can be completed.
</context>

<next_action>
Start with: Place 20+ real insurance PDFs in pdfs-to-test/ (2 per insurer), then run:
  poliza-extractor batch-fixtures pdfs-to-test/ --run-tests
</next_action>
