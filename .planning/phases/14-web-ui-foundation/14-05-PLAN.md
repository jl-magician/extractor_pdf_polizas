---
phase: 14-web-ui-foundation
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified:
  - tests/test_ui_integration.py
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-05
  - UI-06
must_haves:
  truths:
    - "User can drag-and-drop a PDF in the browser, submit it, and watch an inline status indicator update to complete without refreshing (ROADMAP SC-1)"
    - "User can browse a paginated poliza list filtered by aseguradora, date range, and free-text search, and open any record to see all extracted fields (ROADMAP SC-2)"
    - "User can view a dashboard showing total polizas processed, warning count, and records with validation warnings (ROADMAP SC-3)"
    - "User can download a poliza as Excel or JSON from the detail page without a terminal (ROADMAP SC-4)"
    - "Uploaded PDFs are retained at data/pdfs/{poliza_id}.pdf and accessible after job expires from memory (ROADMAP SC-5)"
    - "All 5 pages render with sidebar navigation containing Dashboard, Subir PDFs, Polizas, Historial de Lotes"
    - "Full test suite passes with no regressions from prior phases"
  artifacts:
    - path: "tests/test_ui_integration.py"
      provides: "Integration tests verifying all pages render and navigation works"
  key_links:
    - from: "policy_extractor/api/__init__.py"
      to: "all UI routers"
      via: "app.include_router"
      pattern: "include_router"
---

<objective>
Run full integration test suite and verify all 5 pages work together. Human checkpoint for visual verification.

Purpose: Confirm the complete web UI is functional end-to-end before marking the phase complete.
Output: All tests green, visual confirmation from user.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-web-ui-foundation/14-CONTEXT.md
@.planning/phases/14-web-ui-foundation/14-01-SUMMARY.md
@.planning/phases/14-web-ui-foundation/14-02-SUMMARY.md
@.planning/phases/14-web-ui-foundation/14-03-SUMMARY.md
@.planning/phases/14-web-ui-foundation/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests verifying all pages and run full test suite</name>
  <files>tests/test_ui_integration.py</files>
  <read_first>
    policy_extractor/api/__init__.py,
    tests/test_ui_infra.py,
    tests/test_ui_upload.py,
    tests/test_ui_pages.py,
    tests/test_ui_dashboard.py
  </read_first>
  <action>
    Create **tests/test_ui_integration.py** -- integration tests using TestClient that verify:

    1. **Navigation:** All 5 sidebar links are present in every page response:
       - GET / contains href="/", href="/subir", href="/ui/polizas", href="/ui/lotes"
    2. **Page routing:** All routes return 200:
       - GET / returns 200 (dashboard)
       - GET /subir returns 200 (upload)
       - GET /ui/polizas returns 200 (poliza list)
       - GET /ui/lotes returns 200 (job history)
    3. **Active page indicator:** Each page highlights the correct sidebar item:
       - GET / response contains active state class for dashboard nav
    4. **CDN integrity:** Every page includes both CDN tags:
       - `tailwindcss/browser@4` and `htmx.org@2.0.8`
    5. **Spanish UI:** All pages use Spanish copy (no English-only labels)
    6. **ROADMAP success criteria coverage:**
       - SC-1: GET /subir contains hx-post (upload submission endpoint exists)
       - SC-2: GET /ui/polizas contains poliza-rows and search input
       - SC-3: GET / contains "Total Polizas" and "Requieren revision"
       - SC-4: Poliza detail page contains "Descargar Excel" and "Descargar JSON"
       - SC-5: Upload module contains PDFS_RETENTION_DIR
    7. **Full suite run:** After integration tests pass, run `python -m pytest tests/ -x` to confirm no regressions across all test files

    Use in-memory SQLite with Base.metadata.create_all pattern. Seed minimal data where needed.
  </action>
  <verify>
    <automated>cd C:/Users/josej/extractor_pdf_polizas && python -m pytest tests/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ui_integration.py exits 0
    - `python -m pytest tests/ -x` exits 0 (full suite green)
    - tests/test_ui_integration.py contains assertions for all 4 page routes (/, /subir, /ui/polizas, /ui/lotes)
    - tests/test_ui_integration.py contains assertion for CDN tags
    - tests/test_ui_integration.py contains assertions referencing ROADMAP success criteria behaviors
  </acceptance_criteria>
  <done>All integration tests pass. Full test suite green with no regressions. Tests cover all 5 ROADMAP success criteria behaviors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete web UI</name>
  <files>none</files>
  <action>
    User visually verifies the complete web UI by starting the server and checking all 5 pages in the browser.
  </action>
  <verify>User confirms all pages render correctly with Spanish text, sidebar navigation, and correct styling.</verify>
  <done>User types "approved" confirming the UI is visually correct and functional.</done>
  <what-built>
    Complete web UI with 5 pages: Dashboard (landing), Upload (batch workflow), Poliza List (search/filter/pagination), Poliza Detail (field display/export), and Job History (batch tracking). All use HTMX + Jinja2 + Tailwind CSS on the existing FastAPI backend.
  </what-built>
  <how-to-verify>
    1. Start the server: `cd C:/Users/josej/extractor_pdf_polizas && python -m uvicorn policy_extractor.api:app --reload`
    2. Open http://localhost:8000 in browser
    3. Verify Dashboard page: sidebar visible on left (dark background), 3 stat cards showing (Total Polizas, Advertencias, Score Promedio), "Requieren revision" section, date range buttons AND custom desde/hasta date inputs (D-17)
    4. Click "Subir PDFs" in sidebar: verify upload page with drag-and-drop zone, file input, batch name field, "Procesar Lote" button
    5. Click "Polizas" in sidebar: verify poliza list with table, filter toggle button, search input
    6. Click "Historial de Lotes" in sidebar: verify job history page
    7. Check sidebar highlights the active page
    8. Check all text is in Spanish
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes: `python -m pytest tests/ -x`
- User visually confirms all 5 pages render correctly
- Sidebar navigation works across all pages
</verification>

<success_criteria>
- All tests pass including integration tests
- User approves the visual layout and functionality
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-ui-foundation/14-05-SUMMARY.md`
</output>
