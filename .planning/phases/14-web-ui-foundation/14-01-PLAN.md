---
phase: 14-web-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - policy_extractor/config.py
  - policy_extractor/storage/models.py
  - policy_extractor/api/__init__.py
  - policy_extractor/api/ui/__init__.py
  - policy_extractor/templates/base.html
  - policy_extractor/static/.gitkeep
  - alembic/versions/004_batch_jobs.py
  - tests/test_ui_infra.py
autonomous: true
requirements:
  - UI-06
must_haves:
  truths:
    - "FastAPI app serves HTML pages with sidebar layout at GET /"
    - "BatchJob table exists in database after migration"
    - "Jinja2 templates render with HTMX and Tailwind CDN scripts in head"
  artifacts:
    - path: "policy_extractor/templates/base.html"
      provides: "Sidebar layout shell with CDN script tags"
      contains: "tailwindcss/browser@4"
    - path: "policy_extractor/storage/models.py"
      provides: "BatchJob ORM model"
      contains: "class BatchJob"
    - path: "alembic/versions/004_batch_jobs.py"
      provides: "Migration for batch_jobs table"
      contains: "batch_jobs"
    - path: "policy_extractor/api/ui/__init__.py"
      provides: "Shared templates instance for UI routers"
      exports: ["templates"]
  key_links:
    - from: "policy_extractor/api/__init__.py"
      to: "policy_extractor/templates/"
      via: "Jinja2Templates mount"
      pattern: "Jinja2Templates"
    - from: "policy_extractor/api/__init__.py"
      to: "policy_extractor/static/"
      via: "StaticFiles mount"
      pattern: "StaticFiles"
---

<objective>
Set up the web UI infrastructure: add jinja2 dependency, create base template with sidebar navigation, mount Jinja2Templates and StaticFiles in FastAPI, create BatchJob ORM model with Alembic migration, and add a REVIEW_SCORE_THRESHOLD config value.

Purpose: Every subsequent plan depends on the template engine, base layout, and BatchJob model. This plan creates the foundation.
Output: Working base layout at GET /, BatchJob table in DB, shared templates instance for UI routers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-web-ui-foundation/14-CONTEXT.md
@.planning/phases/14-web-ui-foundation/14-RESEARCH.md
@.planning/phases/14-web-ui-foundation/14-UI-SPEC.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From policy_extractor/api/__init__.py:
```python
app = FastAPI(title="Poliza Extractor API", version="1.0.0", ...)
def get_db() -> Generator[Session, None, None]: ...
DbDep = Annotated[Session, Depends(get_db)]

@app.on_event("startup")
def on_startup() -> None:
    engine = init_db(settings.DB_PATH)
    SessionLocal.configure(bind=engine)
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)
```

From policy_extractor/storage/models.py:
```python
class Base(DeclarativeBase): pass
class Poliza(Base): __tablename__ = "polizas"  # id, numero_poliza, aseguradora, ...
class Asegurado(Base): __tablename__ = "asegurados"
class Cobertura(Base): __tablename__ = "coberturas"
class IngestionCache(Base): __tablename__ = "ingestion_cache"
```

From policy_extractor/config.py:
```python
class Settings:
    DB_PATH: str = os.getenv("DB_PATH", "data/polizas.db")
    PROJECT_ROOT: Path = Path(__file__).parent.parent
    # ... other settings
settings = Settings()
```

From policy_extractor/storage/database.py:
```python
def init_db(db_path: str = "data/polizas.db"): ...
SessionLocal = sessionmaker(autocommit=False, autoflush=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add jinja2 dependency, BatchJob model, migration, config, and shared templates module</name>
  <files>
    pyproject.toml,
    policy_extractor/config.py,
    policy_extractor/storage/models.py,
    alembic/versions/004_batch_jobs.py,
    policy_extractor/api/ui/__init__.py,
    tests/test_ui_infra.py
  </files>
  <read_first>
    pyproject.toml,
    policy_extractor/config.py,
    policy_extractor/storage/models.py,
    policy_extractor/storage/database.py,
    policy_extractor/api/__init__.py,
    alembic/versions/003_validation_warnings.py
  </read_first>
  <behavior>
    - Test 1: BatchJob model has all required columns (id, batch_name, status, total_files, completed_files, failed_files, created_at, completed_at, results_json)
    - Test 2: BatchJob default status is "pending"
    - Test 3: REVIEW_SCORE_THRESHOLD setting defaults to 0.70
    - Test 4: Alembic migration 004 creates batch_jobs table with inspector guard
  </behavior>
  <action>
    1. **pyproject.toml** — Add `"jinja2>=3.1.6"` to the `dependencies` list (after `"openpyxl>=3.1.5"`).

    2. **policy_extractor/config.py** — Add to Settings class:
       ```python
       REVIEW_SCORE_THRESHOLD: float = float(os.getenv("REVIEW_SCORE_THRESHOLD", "0.70"))
       ```

    3. **policy_extractor/storage/models.py** — Add BatchJob model after IngestionCache class:
       ```python
       class BatchJob(Base):
           __tablename__ = "batch_jobs"

           id: Mapped[str] = mapped_column(String(36), primary_key=True)
           batch_name: Mapped[Optional[str]] = mapped_column(String, nullable=True)
           status: Mapped[str] = mapped_column(String, default="pending")
           total_files: Mapped[int] = mapped_column(default=0)
           completed_files: Mapped[int] = mapped_column(default=0)
           failed_files: Mapped[int] = mapped_column(default=0)
           created_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
           completed_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
           results_json: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
       ```

    4. **alembic/versions/004_batch_jobs.py** — Create migration following established pattern from 002/003. Use `render_as_batch=True` in `op.batch_alter_table`. Include inspector guard: `if "batch_jobs" not in inspector.get_table_names()`. Revision ID: generate a unique 12-char hex. Down revision: read from 003 file. Create table with all columns matching the ORM model above. `server_default="pending"` for status, `server_default="0"` for integer counters.

    5. **policy_extractor/api/ui/__init__.py** — Create shared templates module:
       ```python
       """Shared Jinja2Templates instance for all UI route modules."""
       from pathlib import Path
       from fastapi.templating import Jinja2Templates

       _TEMPLATES_DIR = Path(__file__).resolve().parent.parent.parent / "templates"
       templates = Jinja2Templates(directory=str(_TEMPLATES_DIR))
       ```

    6. **tests/test_ui_infra.py** — Create tests verifying BatchJob model columns, default values, and config threshold. Use in-memory SQLite with `Base.metadata.create_all(engine)`. Import BatchJob from models, create instance, verify defaults.
  </action>
  <verify>
    <automated>cd C:/Users/josej/extractor_pdf_polizas && python -m pytest tests/test_ui_infra.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `"jinja2>=3.1.6"`
    - policy_extractor/config.py contains `REVIEW_SCORE_THRESHOLD: float = float(os.getenv("REVIEW_SCORE_THRESHOLD", "0.70"))`
    - policy_extractor/storage/models.py contains `class BatchJob(Base):`
    - policy_extractor/storage/models.py contains `__tablename__ = "batch_jobs"`
    - policy_extractor/storage/models.py contains `status: Mapped[str] = mapped_column(String, default="pending")`
    - alembic/versions/004_batch_jobs.py contains `"batch_jobs"` and `inspector.get_table_names()`
    - policy_extractor/api/ui/__init__.py contains `templates = Jinja2Templates(`
    - tests/test_ui_infra.py exits 0
  </acceptance_criteria>
  <done>BatchJob ORM model exists with all columns, migration creates batch_jobs table, jinja2 in dependencies, REVIEW_SCORE_THRESHOLD=0.70 in config, shared templates module created</done>
</task>

<task type="auto">
  <name>Task 2: Create base template with sidebar, mount templates and static files in FastAPI, add placeholder dashboard route</name>
  <files>
    policy_extractor/templates/base.html,
    policy_extractor/static/.gitkeep,
    policy_extractor/api/__init__.py,
    tests/test_ui_infra.py
  </files>
  <read_first>
    policy_extractor/api/__init__.py,
    policy_extractor/api/ui/__init__.py,
    .planning/phases/14-web-ui-foundation/14-UI-SPEC.md,
    .planning/phases/14-web-ui-foundation/14-CONTEXT.md
  </read_first>
  <action>
    1. **policy_extractor/static/.gitkeep** — Create empty file so static/ directory exists (Pitfall 6 from RESEARCH.md).

    2. **policy_extractor/templates/base.html** — Create the base layout per UI-SPEC.md Layout section:
       ```html
       <!DOCTYPE html>
       <html lang="es">
       <head>
         <meta charset="UTF-8">
         <meta name="viewport" content="width=device-width, initial-scale=1.0">
         <title>{% block title %}Extractor de Polizas{% endblock %}</title>
         <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
         <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>
       </head>
       <body class="min-h-screen flex bg-gray-50">
         <!-- Sidebar per D-19: always visible, dark bg like Linear/Notion -->
         <nav class="w-60 bg-gray-800 text-gray-100 flex-shrink-0 flex flex-col">
           <div class="p-6">
             <span class="text-xl font-semibold text-white">Extractor de Polizas</span>
           </div>
           <div class="flex-1 flex flex-col gap-1 px-3">
             <a href="/" class="flex items-center gap-3 min-h-[44px] px-3 rounded-md text-sm {% if active_page == 'dashboard' %}border-l-2 border-blue-400 bg-gray-700 text-white{% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}">
               <!-- Heroicon: chart-bar (inline SVG, 20x20) -->
               <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 0 1 3 19.875v-6.75ZM9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 0 1-1.125-1.125V8.625ZM16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 0 1-1.125-1.125V4.125Z"/></svg>
               Dashboard
             </a>
             <a href="/subir" class="flex items-center gap-3 min-h-[44px] px-3 rounded-md text-sm {% if active_page == 'upload' %}border-l-2 border-blue-400 bg-gray-700 text-white{% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}">
               <!-- Heroicon: arrow-up-tray -->
               <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5m-13.5-9L12 3m0 0 4.5 4.5M12 3v13.5"/></svg>
               Subir PDFs
             </a>
             <a href="/ui/polizas" class="flex items-center gap-3 min-h-[44px] px-3 rounded-md text-sm {% if active_page == 'polizas' %}border-l-2 border-blue-400 bg-gray-700 text-white{% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}">
               <!-- Heroicon: document-text -->
               <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z"/></svg>
               Polizas
             </a>
             <a href="/ui/lotes" class="flex items-center gap-3 min-h-[44px] px-3 rounded-md text-sm {% if active_page == 'jobs' %}border-l-2 border-blue-400 bg-gray-700 text-white{% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}">
               <!-- Heroicon: clock -->
               <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/></svg>
               Historial de Lotes
             </a>
           </div>
           <div class="px-6 py-4 text-xs text-gray-500">v2.0</div>
         </nav>
         <!-- Main content area -->
         <main class="flex-1 bg-gray-50 overflow-y-auto">
           <div class="max-w-7xl mx-auto px-8 py-8">
             {% block content %}{% endblock %}
           </div>
         </main>
       </body>
       </html>
       ```

    3. **policy_extractor/api/__init__.py** — Add imports and mounts. After the existing `app = FastAPI(...)` block:
       - Add imports: `from pathlib import Path`, `from fastapi.staticfiles import StaticFiles`, `from fastapi.responses import HTMLResponse`, `from starlette.requests import Request`
       - Define paths: `STATIC_DIR = Path(__file__).resolve().parent.parent / "static"` and `TEMPLATES_DIR = Path(__file__).resolve().parent.parent / "templates"`
       - Create dirs if missing: `STATIC_DIR.mkdir(parents=True, exist_ok=True)`
       - Mount static files: `app.mount("/static", StaticFiles(directory=str(STATIC_DIR)), name="static")`
       - Import the shared templates: `from policy_extractor.api.ui import templates`
       - Add a temporary dashboard route that renders base.html with `active_page="dashboard"` and a simple "Dashboard coming soon" heading in a `dashboard.html` template
       - Create `policy_extractor/templates/dashboard.html` that extends base.html with `{% block content %}<h1 class="text-xl font-semibold text-gray-900">Dashboard</h1><p class="text-sm text-gray-500">Contenido del dashboard se implementara en Plan 04.</p>{% endblock %}`

    4. **tests/test_ui_infra.py** — Add tests (append to existing file from Task 1):
       - Test: GET / returns 200 with `text/html` content type
       - Test: Response body contains `Extractor de Polizas` (app title in sidebar)
       - Test: Response body contains `tailwindcss/browser@4` (CDN tag)
       - Test: Response body contains `htmx.org@2.0.8` (CDN tag)
       - Test: Response body contains `Dashboard` (sidebar nav item)
       - Use FastAPI TestClient pattern from existing tests

    Per D-15: Dashboard is the landing page (GET / route).
    Per D-19: Sidebar always visible, dark bg (bg-gray-800).
    Per D-21: Tailwind CSS via CDN.
    Per D-23: HTMX + Jinja2 on existing FastAPI.
    Per D-22: UI language Spanish.
    Per UI-SPEC: CDN tags exactly as specified, sidebar w-60 bg-gray-800.
  </action>
  <verify>
    <automated>cd C:/Users/josej/extractor_pdf_polizas && python -m pytest tests/test_ui_infra.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - policy_extractor/templates/base.html contains `cdn.jsdelivr.net/npm/@tailwindcss/browser@4`
    - policy_extractor/templates/base.html contains `cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js`
    - policy_extractor/templates/base.html contains `w-60 bg-gray-800`
    - policy_extractor/templates/base.html contains `Dashboard` and `Subir PDFs` and `Polizas` and `Historial de Lotes`
    - policy_extractor/templates/base.html contains `lang="es"`
    - policy_extractor/api/__init__.py contains `StaticFiles(directory=`
    - policy_extractor/api/__init__.py contains `from policy_extractor.api.ui import templates`
    - policy_extractor/templates/dashboard.html contains `{% extends "base.html" %}`
    - policy_extractor/static/.gitkeep exists
    - tests/test_ui_infra.py exits 0
  </acceptance_criteria>
  <done>Base template renders with sidebar, CDN tags, Spanish nav labels. GET / returns 200 HTML. Static files mounted. Templates shared via ui/__init__.py module.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ui_infra.py -x -v` — all infrastructure tests pass
- `python -m pytest tests/ -x` — full test suite still passes (no regressions)
- `pip install -e .` — jinja2 installs successfully
- `python -c "from policy_extractor.storage.models import BatchJob; print(BatchJob.__tablename__)"` — prints "batch_jobs"
</verification>

<success_criteria>
- GET / returns 200 with HTML containing sidebar navigation and CDN script tags
- BatchJob model has all 9 columns (id, batch_name, status, total_files, completed_files, failed_files, created_at, completed_at, results_json)
- Alembic migration 004 creates batch_jobs table
- REVIEW_SCORE_THRESHOLD=0.70 in settings
- Shared templates instance at policy_extractor.api.ui.templates
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-ui-foundation/14-01-SUMMARY.md`
</output>
