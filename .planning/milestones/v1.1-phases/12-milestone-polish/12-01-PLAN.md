---
phase: 12-milestone-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - policy_extractor/regression/field_differ.py
  - tests/test_regression_helpers.py
autonomous: true
requirements: [REG-02]
gap_closure: true

must_haves:
  truths:
    - "FieldDiffer compares Decimal-originated monetary fields without spurious FAIL from float serialization roundtrip"
    - "Exact match semantics preserved for all non-monetary scalar fields per user decision in Phase 11"
    - "Existing FieldDiffer tests still pass unchanged"
  artifacts:
    - path: "policy_extractor/regression/field_differ.py"
      provides: "Safe Decimal/float comparison via string normalization"
      contains: "_values_equal"
    - path: "tests/test_regression_helpers.py"
      provides: "Tests proving Decimal-float roundtrip does not cause spurious FAIL"
      contains: "test_.*decimal"
  key_links:
    - from: "policy_extractor/regression/field_differ.py"
      to: "tests/test_regression_helpers.py"
      via: "FieldDiffer import and compare() call"
      pattern: "FieldDiffer.*compare"
---

<objective>
Fix FieldDiffer to handle Decimal-to-float serialization roundtrip safely for monetary fields (prima_total, suma_asegurada, deducible).

Purpose: When PolicyExtraction.model_dump(mode="json") serializes Decimal("1500.00") to 1500.0 (float), and a subsequent extraction produces Decimal("1500.00") -> 1500.0, the values should compare equal. The current strict `!=` operator can fail on float representation artifacts (e.g., 1500.0 vs 1500.00 as strings, or Decimal vs float type mismatch).

Output: Updated field_differ.py with a `_values_equal` helper that normalizes numeric comparison, plus new tests proving the fix.
</objective>

<execution_context>
@C:/Users/josej/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/josej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/11-regression-suite/11-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From policy_extractor/schemas/poliza.py:
```python
prima_total: Optional[Decimal] = None  # Decimal field — becomes float in model_dump(mode="json")
```

From policy_extractor/schemas/cobertura.py:
```python
suma_asegurada: Optional[Decimal] = None  # Decimal field
deducible: Optional[Decimal] = None       # Decimal field
```

From policy_extractor/regression/field_differ.py (current comparison at line 111):
```python
act_val = self._actual.get(key)
if exp_val != act_val:
    report.rows.append((key, exp_val, act_val, "FAIL"))
```
Same pattern at line 143 (campos_adicionales) and line 210 (list sub-fields).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _values_equal helper and Decimal-safe comparison tests</name>
  <files>policy_extractor/regression/field_differ.py, tests/test_regression_helpers.py</files>
  <read_first>
    - policy_extractor/regression/field_differ.py (full file — 213 lines)
    - tests/test_regression_helpers.py (full file — 322 lines)
    - policy_extractor/schemas/poliza.py (line 35: prima_total Decimal field)
    - policy_extractor/schemas/cobertura.py (lines 11-12: suma_asegurada, deducible Decimal fields)
  </read_first>
  <behavior>
    - Test 16: FieldDiffer with expected prima_total=1500.0 (float from JSON roundtrip) and actual prima_total=1500.0 produces no FAIL
    - Test 17: FieldDiffer with expected prima_total="1500.00" (string) and actual prima_total=1500.0 (float) produces FAIL (different types, not a Decimal roundtrip issue — exact match)
    - Test 18: FieldDiffer with expected suma_asegurada=500000.0 and actual suma_asegurada=500000.0 inside coberturas produces no FAIL
    - Test 19: FieldDiffer with Decimal(\"1500.00\") vs float 1500.0 comparison — no spurious FAIL (the core roundtrip case)
    - Test 20: FieldDiffer with truly different monetary values (1500.0 vs 1600.0) still produces FAIL
  </behavior>
  <action>
    IMPORTANT: The user chose "Exact match" in Phase 11 discuss-phase. Do NOT add general numeric tolerance. The fix is narrow: handle the specific Decimal-to-float serialization artifact.

    1. In field_differ.py, add a module-level helper function `_values_equal(expected, actual) -> bool`:
       - If both values are numeric (int or float) OR one is Decimal and the other is int/float:
         Use `math.isclose(float(expected), float(actual), rel_tol=1e-9)` for very tight comparison.
         This handles Decimal("1500.00") vs 1500.0 without false positives on truly different values.
       - For all other types: use strict equality (`expected == actual`)
       - Import `math` at top of file and `from decimal import Decimal`

    2. Replace all three `!=` comparison sites with `not _values_equal(...)`:
       - Line 111: `if exp_val != act_val:` -> `if not _values_equal(exp_val, act_val):`
       - Line 143: `elif exp_val != act_val:` -> `elif not _values_equal(exp_val, act_val):`
       - Line 210: `if exp_sub_val != act_sub_val:` -> `if not _values_equal(exp_sub_val, act_sub_val):`

    3. Add tests 16-20 in TestFieldDiffer class in test_regression_helpers.py, continuing the numbering convention (test_16_*, test_17_*, etc.). Import Decimal in the test file.

    Note: rel_tol=1e-9 is extremely tight — it only forgives representation artifacts like 1500.0000000000001 vs 1500.0. Truly different values like 1500.0 vs 1500.01 will still FAIL. This preserves the user's "exact match" intent.
  </action>
  <verify>
    <automated>python -m pytest tests/test_regression_helpers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "_values_equal" policy_extractor/regression/field_differ.py returns at least 4 matches (1 definition + 3 call sites)
    - grep "math.isclose" policy_extractor/regression/field_differ.py returns 1 match
    - grep "rel_tol=1e-9" policy_extractor/regression/field_differ.py returns 1 match
    - grep "test_16\|test_17\|test_18\|test_19\|test_20" tests/test_regression_helpers.py returns 5 matches
    - python -m pytest tests/test_regression_helpers.py -x -q passes with 0 failures
    - python -m pytest tests/ -x -q --ignore=tests/test_regression.py passes with 0 failures
  </acceptance_criteria>
  <done>FieldDiffer handles Decimal/float monetary comparison without spurious failures. All 20+ regression helper tests pass. Full test suite green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_regression_helpers.py -x -q` — all tests pass including new Decimal tests
- `python -m pytest tests/ -x -q --ignore=tests/test_regression.py` — full suite green
- `grep "_values_equal" policy_extractor/regression/field_differ.py` — function defined and used at all 3 comparison sites
</verification>

<success_criteria>
- FieldDiffer.compare() returns no FAIL when Decimal("1500.00") is compared against float 1500.0
- FieldDiffer.compare() still returns FAIL when values truly differ (1500.0 vs 1600.0)
- All existing tests pass without modification
- No general tolerance added — only Decimal/float serialization artifact handled
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-polish/12-01-SUMMARY.md`
</output>
